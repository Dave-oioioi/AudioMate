# AudioMate v0.1.10

## Changes

- Removed manual process-name add fields from the music/BGM and excluded-app settings pages.
- Kept search inside the scan picker so typing filters discovered apps instead of directly adding unknown names.
- Prioritized Windows volume-mixer/audio-session apps in scan results before broader PC application discovery.
- Preserved scan-result order through candidate filtering so the mixer-first priority stays visible.

## Build

```powershell
powershell -ExecutionPolicy Bypass -File scripts\publish-release.ps1 -Version 0.1.10
```
